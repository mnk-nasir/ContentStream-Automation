# LLM / OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional default documents (you can also pass files via CLI)
DEFAULT_SYSTEM_PROMPT=
DEFAULT_SCHEMA_DOC=

# IMGBB (for image uploads)
IMGBB_API_KEY=

# Notifications (optional)
TELEGRAM_CHAT_ID=

# Where outputs are saved
OUTPUT_DIR=outputs
